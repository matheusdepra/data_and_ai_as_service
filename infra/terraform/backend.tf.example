terraform {
  # Backend configuration cannot use variables.
  # Copy this file to backend.tf and fill in the values for your org/project.
  backend "gcs" {
    bucket = "REPLACE_WITH_TF_STATE_BUCKET"
    prefix = "dativerso/infra/terraform"
  }
}

